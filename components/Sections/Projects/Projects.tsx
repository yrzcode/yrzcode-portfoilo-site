import { Cards } from "./ProjectCards/Cards";
import { Title } from "./Title";

export const Projects = () => {
  return (
    <section id="projects">
      <Title />
      <Cards />
    </section>
  );
};
